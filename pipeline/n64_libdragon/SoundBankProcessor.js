class N64LibDragonSoundBankProcessor {
    _environment;

    constructor(environment) {
        this._environment = environment;
    }

    async process(soundBank) {
        
    }
}

module.exports = N64LibDragonSoundBankProcessor;